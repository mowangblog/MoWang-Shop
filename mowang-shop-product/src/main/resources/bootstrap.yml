spring:
  application:
    name: mowang-shop-product
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848 #Nacos作为配置中心地址
        file-extension: yml #指定yml格式的配置
        namespace: product
  jackson:
    date-format: yyyy-MM-dd HH-mm-ss

server:
  port: 3333
